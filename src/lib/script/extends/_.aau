namespace script.extends;

require = function (filename, args, code) {
	if (code === null)
		filename = ..io.fullpath(filename);
	return scriptObject.dofile(filename, code, args);
}

/*导入其他扩展库{{*/
import script.extends.html
/*}}*/
